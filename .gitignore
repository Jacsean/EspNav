# ---- Reasonix IDE 工作区元数据（工具生成，与本项目无关）----
# 说明：Esp32Nav/ 是 AI 助手的工作目录，里面的 reasonix.toml 记的是本机路径与
# 历史命令白名单，不应该进版本库；.reasonix/ 是它的运行数据（任务/附件）。
Esp32Nav/
.vscode/

# ---- 常见临时产物 ----
*.log
*.tmp
*~

# ---- 构建产物 / 交付产物（不入库）----
release/
release*/
out/
_wt_*/

# ---- 静态图导航演示素材（用户提供，不入库）----
docs/nav_*.jpg
