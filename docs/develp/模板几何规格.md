# 模板几何规格（草案 V0.1）— H1-A 参数化几何层

> - 状态：**草案待评审**（2026-09-03）。本文件是审查决策 H1-A 的产物，评审通过后才进入 v2 参数化改造与 ESP32 C 版移植。
> - 关联文档：`ble_protocol.md` V1.7 §3.1.1（字段与 type×dir 组合表）；`技术实现路径.md` §1.5/§1.6（投影常量、切换时序）。
> - 视觉验收基准：`nav_sim_v2.html`（主视图集成、近大远小）与 `nav_sim_v3.html`（平面整屏样例）。

## 0. 目的与范围

打通三段参数化链路，替代当前"写死坐标 CASE"（v2 `ROAD_CASES` / v3 `CASES`）：

```
手机端（生成器）            帧 road 字段              渲染端（解释器）
导航真实走向 ──► type/dir/exits/pts/cx/cy/r/half ──► 路网+绿路径+车标
                                                    （HTML V2 参数化版 = C 版 1:1 基准）
```

- **手机端生成器**：由导航 SDK 机动与路线走向，产出协议字段（见 §6）。
- **渲染端解释器**：只读字段参数化生成画面，不解释业务语义（见 §5）。

## 1. 坐标系与公共常量（两端地基）

1. 平面坐标：**车头朝上**屏幕像素系，x: 0~319（右为正），y: 0~239（下为正）；车辆自**底部南入口**进入画面；N=上、E=右、W=左、S=下。
2. 投影（V2 冻结）：`PROJ={cx:160, yNear:130, yFar:20, sNear:1.0, sFar:0.24}`，深度 y 线性缩放；主视图绘制前另施"底部生长"缩放（锚 y=22、k=1.1）。常量以 `nav_sim_v2.html` `PROJ`/`drawScene` 为准（技术路径 §1.5 已同步）。
3. 半宽体系（决策 M2-A，保留两套、文档化口径）：
   - 行驶条带：近宽 150px→远 36px，由发送端 `centerLine` 首末点直接决定；
   - 模板体系：基准半宽 `road.half`（缺省 62px，近端视觉宽≈124px·k≈136），支路半宽按 §5.4 系数缩放；
   - 两体系**同轴不同宽**，衔接处（y≈130-134）的宽度不连续由手机端 centerLine 近端坐标与模板南引路协同对齐（技术路径 §1.6 帧序 2 口径）。

## 2. 帧字段 → 几何参数映射（V1.7）

| 字段 | 用途 | 缺省 |
| --- | --- | --- |
| `type` | 模板类别（7 类） | 无（缺省=行驶条带） |
| `dir` | 车头相对机动方向 `straight/left/right/exitN` | straight |
| `exits` |（roundabout/multi）候选出口方位数组：roundabout=**不含入口**、按绕行方向排列（exits[0]=进入后第 1 岔路）；multi=候选支路方位集合 | roundabout 必填；multi 缺省=渲染端五方向示意 |
| `pts` | 导航真实走向平面折线（≤16 点），含环岛内离散弧 | 无（缺省=主路直线） |
| `cx/cy/r` | roundabout 平面圆心与半径 | 160 / 84 / 52（平面值，见 §5.5） |
| `half` | 路面基准半宽（平面 px） | 62 |
| `maneuver` | 顶部提示/符号与机动口径 | 无 |

> 约束：`pts` 首点应位于南入口引路近端（y≈134 平面带），末点位于出口方向；`pts` 点数 ≤16 与 `centerLine` 同限。

## 3. type×dir×exits 语义（H2-A）

### 3.1 合法组合（与 ble_protocol V1.7 §3.1.1 一致）

| `type` | 合法 `dir` | 几何要点 |
| --- | --- | --- |
| `straight` | （缺省） | 直路，条带体系即可；一般不需模板 |
| `curve` | `left / right` | 单弯，路径 pts 为弯折线 |
| `tjunc` | `left / right` | T 口，支路在左/右，必转向 |
| `cross` | `straight / left / right` | 十字四向 |
| `multi` | （缺省） | 多岔，支路方向由 pts 尾段推断 |
| `roundabout` | `exitN` | 目标出口 = `exits[N-1]`（见 3.2） |
| `fork` | `left / right` | 主路直行 + 单侧匝道驶出 |

### 3.2 roundabout 的 `exitN` / `exits` 口径与弧向（O1 已裁决 2026-09-03，方案 1：数据驱动 + 方向弧）

- `exits`：**不含南入口**的可驶出口方位数组，**按车辆实际绕行方向排列**（`exits[0]` = 进入环岛后第 1 个可驶出口）。手机端依据导航 SDK 路线确定绕行方向并排序后下发。
- `dir=exitN`：N = **exits 下标 + 1**（纯渲染/几何口径，1 起，不含入口）——渲染端唯一的出口定位依据：目标方位 = `exits[N-1]`。
- `maneuver=roundabout_N`：N = **导航 SDK 播报原文口径**（与语音一致），仅供顶部文字/符号；渲染端**不**据此定位出口。含数字的提示文本（"环岛第 N 出口驶出"）由手机端按 SDK 原文组装进 `hint`，避免双口径数字显示歧义。
- SDK 适配（手机端，阶段4 实测）：记 SDK 出口计数是否含入口（高德/百度可能不同），换算 `dir_N = SDK_N − (SDK 含入口 ? 1 : 0)`；绕行方向由 SDK 路线确定（中国右行环岛通常逆时针：屏幕系角度增大方向，自南入先遇 W）。
- 弧向：渲染端绿路径 = 自南入口**沿 exits 序列指示的绕行方向**画弧至目标出口（全弧，可能 >90°/180°），**禁用 v2/v3"最近短弧"**——短弧仅当目标恰为第 1 出口（相邻）时与方向弧重合。
- ⚠️ v2 `rbtCase` / v3 样例当前为"短弧 + 含入口编号 + 顺时针示例"，与本节不符，属**待更新**（黄区代码任务，需重验收长弧观感）；样例数据按真实绕行语义重标。
### 3.3 uturn（O4 已裁决 2026-09-03：不进模板库，维持条带 + U 弧 pts）

- `maneuver=uturn`：建议**不带 `road`**——行驶条带 + 顶部"掉头"符号 + `routeCenter` 内 U 形弧折线（手机端生成弧线点）；不套任何路口模板。
- 若确需放大造型，允许 `road.type=curve` + U 形 `pts`（几何仍按弯道画，[TODO] 专用造型待定）。

## 4. `pts` 语义与绿路径

1. `pts` = 该路况段内**导航真实走向**的平面折线（含路口内转向、环岛内离散弧），与路网分离：路网是背景示意，`pts` 为准（协议叠加规则）。
2. 渲染端：`pts` 经 `proj` 透视后，暗绿打底 + 亮绿描边绘制（同 `navPath`），始终最上层；车辆光标位置：
   - 帧含 `pos` 且在模板段内 → 车标 = `pos`（手机端在转向过程中沿 pts 推进 pos，对应技术路径 §1.6 帧序 3）；
   - `pos` 缺省 → 南入口引路起点（≈ v2 冻结 `TP_CAR` 语义：较 pts 起点上移约一个车身，路径尾段在车后可见）。
3. `centerLine` 在模板段内可继续发送，渲染端忽略条带绘制（避免双层路面），切回行驶条带时恢复使用。

## 5. 渲染端生成器规则（草案：按 type 产出画面）

统一管线：`readRoad(f)` → 计算路网支路中心线集合 → 各支路 `fatten`+边线（导航道路流动虚线/非导航静态，V1.6 冻结）→ 路口面片 → 叠加绿路径（帧 `pts`）→ 车标。

### 5.1 通用几何量

- 南入口引路：`[[160, 134+], C]`，半宽 = `half`，**流动**（必经，当前路延续）。
- 路口中心 `C`：由 `pts` 首个显著折点求取；`pts` 无折（straight/curve 缓弯）时不生成路口面片、仅沿 pts 画路。
- 支路出口：到屏幕边（或视野上缘 y≈22）截断。

### 5.2 各 type 草案

| type | 路网（支路） | 选中路径 |
| --- | --- | --- |
| `tjunc(dir)` | 南引路 + 单侧支路（dir=left 向左、right 向右），半宽 side | 南引路→C→支路（流动） |
| `cross(dir)` | 四向支路；未选中支路静态、选中方向流动（V1.6：仅导航道路流动虚线） | 南引路→C→dir 方向 |
| `multi` | 南引路 + 候选支路（读 `road.exits`，缺省渲染端五方向组）；pts 尾段方向匹配选中支路流动，其余静态 | 南引路→C→选中支路/pts 尾段 |
| `fork(dir)` | 主路直行（静态边线 + 车道中线，非导航）+ 分叉匝道（半宽 side-δ，沿 dir 侧） | 匝道 pts |
| `curve(dir)` | 仅沿 pts 画路（roadStraight 类，半宽 half） | pts |
| `straight` | 同 curve | pts |

### 5.3 roundabout（帧参数驱动，O2 已裁决 2026-09-03，方案 A：观感参数 + 冻结比率表）

- 观感参数与冻结比率表（比率以 `r`=外环水平半轴为基准；缺省即 v2 冻结观感，两端共享常量块）：

| 量 | 定义 | 系数/取值 |
| --- | --- | --- |
| `cx` / `cy` | 观感椭圆中心（`road.cx/cy`） | 缺省 160 / 84 |
| `r` | **观感外环水平半轴 a**（`road.r`） | 缺省 58 |
| 外环垂直半轴 b | 0.40·r | 23 |
| 内岛半轴 aIn / bIn | 0.66·r / 0.17·r | 38 / 10 |
| 环带中线半轴 aM / bM | (a+aIn)/2、(b+bIn)/2 | 48 / 16.5 |
| 环上支路半宽 rw | 0.31·r | 18 |
| 南引路半宽 | 0.62·r | 36 |

- 路面：`ellipse(cx,cy,r,b)` 灰路面 + `ellipse(cx,cy,aIn,bIn)` 内岛（暗）+ 环带描边。推荐发送端不自定义 `r`（如需缩放应整组等比；观感仅在缺省值下冻结）。
- 支路：按 `exits` 各方位画静态支路（至屏幕边、半宽 rw）；目标出口支路**流动**。南引路 `(160,134) → (cx,cy+b)`、半宽 0.62·r、流动。
- 方位角表（屏幕系，y 向下，θ 代入 `(a·cosθ, b·sinθ)`，顺时针为正）：E=0°、SE=45°、S=90°、SW=135°、W=180°、NW=225°、N=270°、NE=315°（=-45°）。与 v2（S=90°/E=0°/N=-90°）及 v3（NE=-45°）兼容。
- 绿路径：`(160,134) → (cx,cy+b)` → 沿环带中线（aM/bM）由 θ=90°(S) **沿绕行方向**至目标出口方位角（椭圆参数角离散 16 段，与 C 一致）→ 驶出至屏幕边。绕行方向判据：`exits[0]` 方位角相对 S 的带符号角差（正=顺时针、负=逆时针），全程同向累计，**不做最近短弧**。
- ⚠️ 方向弧使 N≥2 出口的绿路径可能 >180°，与 v2 现有象限短弧观感不同（黄区重验收项）。
### 5.4 半宽系数（O5 已定稿 2026-09-03；v2 TPL.ring=27 预留未用）

| 量 | 值（相对 `half`） | @half=62 |
| --- | --- | --- |
| 主通行半宽 | `half` | 62 |
| 支路 side | `0.56·half`（= v2 TPL.side 35） | 35 |
| 匝道/环岛支路 ring | `0.44·half` | 27 |
| 匝道本身（fork 路面） | `side-6`（v2 冻结） | 29 |
> roundabout 例外：环带/环上支路/南引路半宽以 `r` 为基准（见 §5.3），不套用本表——v2 冻结中 TPL.main=62 与环岛南引路 36 并存属**有意差异**（决策 M2-A），规格层照实记录。

## 6. 手机端生成器职责（阶段 4-1 输入）

1. 输入：SDK 机动事件（类型/环岛出口 N/提示文本）、`turn_dist`、路线 polyline（车前段）。
2. 输出：`type/dir/exits/pts/cx/cy/r/half/maneuver`（映射规则见 §2/§3），以及模板段内逐帧推进的 `pos`。
3. 校验：type×dir 合法（§3.1）；`pts` ≤16 点且界内；`exits` 排序符合绕行方向（§3.2）；非法组合退回"不带 road 的条带帧"（协议兜底）。
4. 时序：沿用技术路径 §1.6（≤200m 进入、150-250ms 淡入、驶出 ≥60m 退出、heading 限速旋转——pts 以进入时刻 heading 为基准生成，段内不随 heading 重投影）。
5. 一致：生成器产出的字段需能通过 HTML V2 参数化版逐帧复现（自动化对照），再进入 C 版。

## 7. v2 参数化改造清单（评审通过后执行，黄区）

1. 新增 `readRoad(f)`：读 `road` 字段 → §5 管线；`ROAD_CASES` 写死坐标函数退役（保留为回归基线若干轮）。
2. 样例/按钮层改为构造 `road` JSON 驱动（v3 各 CASE 平移到 road JSON 样例集，并入主仿真，消除 v2/v3 样例分裂）。
3. 新增"帧↔画面"对照测试：同一 road JSON，旧写死渲染 vs 参数化渲染快照比对（允许以参数化版为准更新基线一次）。
4. 投影/椭圆常量集中为可配置常量块（`PROJ`/`TPL`/环岛系数），供 C 版对照移植。

## 8. 开放问题（评审点）

- ~~O1~~ **已裁决（2026-09-03，方案 1）**：见 §3.2——exits 不含入口、按绕行序；dir=exitN=下标+1；maneuver 保 SDK 口径仅供文字；渲染端方向弧（弃短弧）；SDK 含入口适配集中在手机端（阶段4 实测高德/百度）。
- ~~O2~~ **已裁决（2026-09-03，方案 A）**：见 §5.3——`r` 重定义为观感外环水平半轴 a（缺省 58）+ 冻结比率表（b=0.40r、aIn=0.66r、bIn=0.17r、rw=0.31r、南引路=0.62r，cx/cy 缺省 160/84）。
- ~~O3~~ **已裁决（2026-09-03）**：multi 候选支路方向协议化 = 复用 `road.exits`（可选）；渲染端读 exits 画支路、缺省 v2 冻结五方向组，pts 尾段匹配选中（§2/§5.2、ble_protocol V1.8、v2 multiRoad）。
- ~~O4~~ **已裁决（2026-09-03）**：不进模板库，维持"条带 + routeCenter U 形弧 pts + 顶部符号"（§3.3）。
- ~~O5~~ **已定稿（2026-09-03）**：系数表见 §5.4（main62/side35/fork=side-6；roundabout 走 r 系，TPL.ring 标注预留未用）。
- ~~O6~~ **已裁决（2026-09-03）**：模板态车标优先级 demoCar(HTML 演示) > 帧 `pos`（手机端沿 pts 推进）> 南引路起点（v2 TP_CAR 语义）；见 §4 第 2 条、ble_protocol V1.8、v2 drawScene。
- ~~O7~~ **已结案（2026-09-03）**：机动符号集 {→ ← ↑ ↺ U} 纳入字库扩展清单；maneuver→符号映射见附录 A；HTML 以文字呈现（hint ≤9 全角规则不变）。

---

## 附录 A：maneuver → 顶部符号映射（O7 结案）

| maneuver | 顶部符号（真机字库） | HTML 呈现 |
| --- | --- | --- |
| straight | ↑（或直行） | 文字 |
| turn_left / fork_left | ← | 文字 |
| turn_right / fork_right | → | 文字 |
| roundabout_N | ↺ + N（或文字"环岛第N出口"，数字随 hint） | 文字 |
| multi | 分叉（Y 形） | 文字 |
| curve | →（弧形，可选） | 文字 |
| uturn | U 形弧 | 文字 |

> 符号字形（← → ↑ ↺ 等）纳入 ESP32 字库扩展清单（技术路径 §2.2 特殊字形）；缺字兜底占位方块。

---

## 变更记录

| 日期 | 版本 | 内容 |
| --- | --- | --- |
| 2026-09-03 | V0.3 | C 清零落盘：O3 exits 通用化（roundabout/multi）与 §5.2 multi 规则；O4 uturn 不进模板库；O5 半宽系数定稿；O6 模板态车标 pos 优先；O7 附录 A 符号映射；§8 全部开放问题结案（O1-O7）。 |
| 2026-09-03 | V0.2 | O1 裁决（方案1：exits 不含入口/按绕行序、dir=exitN=下标+1、maneuver 保 SDK 口径、渲染端方向弧、SDK 适配在手机端）；O2 裁决（方案A：r=观感外环水平半轴 + 冻结比率表）；§2 exits 改必填；§5.5 并入 §5.3 |
| 2026-09-03 | V0.1 | 初稿：H1-A 参数化几何层 + H2-A dir/exits 语义；待评审 |
